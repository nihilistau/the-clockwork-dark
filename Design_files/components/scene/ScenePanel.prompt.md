One-sentence: A journal-column surface with a smallcaps heading and an optional iron ledger rule — the frame for the assistant column and character sheet.

```jsx
<ScenePanel title="Traveler" surface="ledger" edge="left">
  <StatLine label="HP" value="14/18" />
</ScenePanel>
```

Surfaces: `ledger | panel | narrative | card`. Edge: `none | left | right` (mirror the side it sits on in the frame). Keep titles to one tracked smallcaps word.
