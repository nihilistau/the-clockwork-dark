{
  "name": "clockwork-frontend-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Unit + e2e tests for The Clockwork Dark frontend (pure helpers, dialog a11y, and a Playwright smoke).",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "jsdom": "^25.0.1",
    "vitest": "^2.1.8"
  }
}
