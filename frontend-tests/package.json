{
  "name": "clockwork-frontend-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Unit tests for The Clockwork Dark pure frontend helpers (clockwork-helpers.js).",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
